using System;

class Program 
{
	public static void Main(string[] args)
	{
	double decimals = 49.04;
		int area = 3;

		Console.WriteLine(decimals / area);
		Console.WriteLine(decimals);
		Console.WriteLine(area * decimals);
		Console.WriteLine(area + decimals / area);
		Console.WriteLine(decimals * decimals * decimals * decimals * decimals * decimals * decimals);
		Console.WriteLine(decimals % area);
	}
}
